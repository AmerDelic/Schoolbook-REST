# error reporting
server.error.include-exception=true
server.error.include-message=always
server.error.include-stacktrace=never
logging.level.org.springframework=TRACE

# socket / path settings
server.port=8081
server.servlet.context-path=/sb

# database
spring.datasource.url=jdbc:mysql://172.17.0.2/schoolbook
spring.datasource.username=root
spring.datasource.password=bazlo0987
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# database - flyway
spring.flyway.enabled=false
spring.flyway.url=jdbc:mysql://172.17.0.2/schoolbook
spring.flyway.user=root
spring.flyway.password=bazlo0987

# other spring settings
spring.jackson.generator.write-bigdecimal-as-plain=true

# security
jwt.provider.secret=${JWT_PROV_SEC}